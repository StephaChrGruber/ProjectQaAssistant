# Mongo
MONGODB_URI=mongodb://mongo:27017
MONGODB_DB=project_qa

# Backend JWT verification for Entra
AZURE_TENANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
AZURE_AUDIENCE=api://YOUR_API_APP_ID_OR_CLIENT_ID
AZURE_ISSUER=https://login.microsoftonline.com/${AZURE_TENANT_ID}/v2.0

# SCIM (shared secret for provisioning service)
SCIM_BEARER_TOKEN=change-me-scim-secret

# Chroma
CHROMA_ROOT=/data/chroma_projects

# LLM (OpenAI compatible; optional)
LLM_BASE_URL=http://host.docker.internal:8001/v1
LLM_API_KEY=whatever
LLM_MODEL=your-model

# CORS / Web
WEB_ORIGIN=http://localhost:3000
